import VisitRegister from "./VisitRegister";
import VisitLogin from "./VisitLogin";

const Hero = (props) => {
  return (
    <div className="hero" id="#Home">
     <VisitRegister />
     <VisitLogin />
    </div>
  );
};

export default Hero;
